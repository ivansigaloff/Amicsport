/**
 * Página principal: renderiza la pantalla de partidos v2 («La Convocatoria»,
 * fichas por día sin 3 columnas). Un solo código para / y /v2 — la
 * implementación vive en app/v2/(tabs)/index.tsx. La versión anterior con
 * layout de 3 columnas queda en el historial de git (tag
 * restauracion-v2-20260704) y en index.v1-backup.tsx.bak.
 */
export { default } from '../v2/(tabs)/index';
