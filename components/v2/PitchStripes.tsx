// Bandas de césped segado del fondo de la demo — implementación web en
// PitchStripes.web.tsx. En nativo no renderiza nada (los screens pintan
// C.bg opaco y una capa fija no puede quedar por debajo sin tocarlos todos).
export default function PitchStripes() {
  return null;
}
